var documenterSearchIndex = {"docs":
[{"category":"section","location":"#SmoreFit","page":"Home","text":"Documentation for SmoreFit.\n\n","title":"SmoreFit"},{"category":"type","location":"#SmoreFit.CMPosteriorResult","page":"Home","text":"CMPosteriorResult\n\nResult of buildPosterior: which CM cohort parameter sets are consistent with the real-world data, scored by the chosen bridge method.\n\nBoth a hard accept/reject set (accepted) and the continuous scores are always stored; the posterior field records which one the caller asked to emphasize. Because cm_sample is retained (with its grid axes for a GridCMSample), acceptance and scores can be reshaped back onto the CM grid via acceptedGrid / scoreGrid.\n\nFor grid-aware results, the per-cohort SM-parameter CI tables (lb_table, ub_table) and a prebuilt CM-grid bounds interpolator (get_bounds) are stored so that interior CM points (not in the cohort) can be queried via inPosterior and posteriorScore. The full uq_results are also retained for inspection and any future re-analysis.\n\nFields\n\ncm_sample — CM parameter points (AbstractCMSample), row-aligned with accepted/scores\ncm_names — CM parameter names (from cm_prior)\naccepted — BitVector; true where the cohort point's consistency score exceeds acceptance_tol\nscores — consistency score per cohort point, in [0, 1]\nbridge — the bridge method used (:box_overlap, :data_trace_in_box, :symmetric_trace)\nposterior — :accept or :graded\nacceptance_tol — threshold used for accepted; default for interior queries\ndata_profiles — the SM profile likelihood computed against the real data (kept for inspection)\nuq_results — the cohort CM-side profiles, one per row of cm_sample.params\nlb_table, ub_table — [n_cohort × n_sm_params] per-cohort SM-parameter CI lower/upper bounds derived from uq_results (with nothing falling back to the profile's swept-range extreme, matching the :box_overlap bridge)\nget_bounds — closure θ_cm -> (lb, ub) interpolating the CI tables across the CM grid; nothing for non-grid layouts\n\nSee also\n\nposteriorSamples, posteriorWeights, acceptedGrid, scoreGrid, inPosterior, posteriorScore.\n\n\n\n\n\n","title":"SmoreFit.CMPosteriorResult"},{"category":"method","location":"#SmoreFit.acceptedGrid-Tuple{CMPosteriorResult}","page":"Home","text":"acceptedGrid(r::CMPosteriorResult) -> Array{Bool}\n\nReshape accepted onto the CM parameter grid. Requires a GridCMSample layout.\n\nExample\n\npost = buildPosterior(sm, data, uq_results, cm_params, cm_prior)   # cm_params a regular grid\nacceptedGrid(post)   # Bool array of size length.(cm_sample.axes)\n\n\n\n\n\n","title":"SmoreFit.acceptedGrid"},{"category":"method","location":"#SmoreFit.buildPosterior-Tuple{SmoreBase.SMFitProblem, Vector{<:SmoreBase.ProfileLikelihoodResult}, SmoreBase.AbstractCMSample, SmoreBase.ParameterPrior}","page":"Home","text":"buildPosterior(problem::SMFitProblem, uq_results, cm_sample, cm_prior; kwargs...) -> CMPosteriorResult\nbuildPosterior(problem::SMFitProblem, uq_results, cm_params::AbstractMatrix, cm_prior; kwargs...)\nbuildPosterior(sm, data, uq_results, cm_sample, cm_prior; loss=GaussianNLL(), kwargs...)\nbuildPosterior(sm, data, uq_results, cm_params::AbstractMatrix, cm_prior; loss=GaussianNLL(), kwargs...)\n\nInfer which CM parameter sets are consistent with real-world data, given the surrogate model and its per-cohort profile likelihood UQ from SmoreBase.\n\nTwo streams of profile information are combined:\n\nCM-side, already done — uq_results[i] is the SM's profile likelihood against the CM cohort point at row i of cm_sample.params. These were produced upstream (e.g. by SmoreBase's _uq over the cohort) and are passed in unchanged.\nData-side, computed here — the SM is fit and then profiled against the real observations. The ingredients for that fit-and-profile step are what problem (or the (sm, data, loss) triple, with sm_prior taken from uq_results[1].fit_result.prior) describe — not anything about the CM cohort, which is fully captured by uq_results.\n\nEach cohort point is then scored by how well its SM-parameter confidence region (from uq_results[i]) overlaps the data's SM-parameter confidence region (from the data profile), via the chosen bridge. Consistent cohort points form the posterior over CM parameter space.\n\nThe CM-side profiles and CM parameters are row-aligned: uq_results[i] ↔ cm_sample.params[i, :] (mirroring SmoreGSA's runSensitivity). A raw matrix is accepted and wrapped with CMSample (from SmoreBase: grid if possible, else scattered).\n\nArguments — describing how to fit/profile the SM against the real data\n\nproblem::SMFitProblem — bundles sm, the real data (single param-set), the SM prior, and the loss to use when fitting and profiling the SM against that data\nor sm::AbstractSurrogateModel + data::AbstractCMData — equivalent to passing a problem built internally as SMFitProblem(sm, data, uq_results[1].fit_result.prior; loss = loss)\n\nArguments — describing the CM cohort already profiled upstream\n\nuq_results::Vector{<:ProfileLikelihoodResult} — one profile likelihood result per cohort point, computed upstream against the cohort's CM-generated data\ncm_sample::AbstractCMSample (or cm_params::AbstractMatrix) — CM parameter locations, row-aligned with uq_results\ncm_prior::ParameterPrior — CM parameter names and support (for labeling the posterior)\n\nKeyword Arguments\n\nbridge::Symbol = :box_overlap — overlap test: :box_overlap (symmetric box-vs-box), :data_trace_in_box (data profile trace points inside the CM box), or :symmetric_trace\nposterior::Symbol = :accept — :accept (hard accept/reject set) or :graded (use scores)\nprofile_options::ProfileLikelihood = ProfileLikelihood() — settings for the data profile (the CM-side profiles in uq_results are already final and not recomputed)\np0 = nothing — initial guess for the data fit ([1 × n_sm_params]); defaults to the column-mean of the cohort SM fits\nloss::AbstractLoss = GaussianNLL() — loss for the data fit, only on the (sm, data, …) forms; the problem form uses problem.loss\nacceptance_tol::Real = 0.0 — a cohort point is accepted iff its score exceeds this; also the default threshold for inPosterior interior queries\ninterp::AbstractCIInterpolator = LinearCIInterp() — how the per-cohort SM-parameter CI bounds are interpolated across the CM grid for interior queries; only consulted when cm_sample isa GridCMSample (interior queries are not built for scattered layouts)\n\nReturns\n\nCMPosteriorResult. Use posteriorSamples for accepted CM sets, posteriorWeights for a graded posterior, and acceptedGrid / scoreGrid for grid-shaped views.\n\nExample\n\n# If you already have an SMFitProblem for the real data in hand:\nproblem = SMFitProblem(sm, data, sm_prior)              # SM + real data + SM prior\npost    = buildPosterior(problem, uq_results, cm_params, cm_prior; bridge = :box_overlap)\n\n# Otherwise, pass sm and the real data directly:\npost = buildPosterior(sm, data, uq_results, cm_params, cm_prior)\n\nposteriorSamples(post)   # CM parameter vectors consistent with the data\n\n\n\n\n\n","title":"SmoreFit.buildPosterior"},{"category":"method","location":"#SmoreFit.inPosterior-Tuple{CMPosteriorResult, AbstractVector}","page":"Home","text":"inPosterior(post::CMPosteriorResult, θ_cm::AbstractVector; tol = nothing) -> Bool\ninPosterior(post::CMPosteriorResult, queries::AbstractMatrix; tol = nothing) -> BitVector\n\nIs the interior CM parameter point in the posterior? Equivalent to posteriorScore(post, …) > tol. tol === nothing (default) uses post.acceptance_tol.\n\nRequires post.cm_sample isa GridCMSample and post.bridge in (:box_overlap, :data_trace_in_box).\n\nExample\n\npost = buildPosterior(sm, data, uq_results, cm_params, cm_prior)   # cm_params a regular grid\ninPosterior(post, [θ1, θ2])\ninPosterior(post, queries; tol = 0.05)\n\n\n\n\n\n","title":"SmoreFit.inPosterior"},{"category":"method","location":"#SmoreFit.posteriorSamples-Tuple{CMPosteriorResult}","page":"Home","text":"posteriorSamples(r::CMPosteriorResult) -> Matrix\n\nThe accepted CM parameter sets, [n_accepted × n_cm_params] — the posterior as a uniform sample set over CM parameter space.\n\nExample\n\npost    = buildPosterior(sm, data, uq_results, cm_params, cm_prior)\nsamples = posteriorSamples(post)   # rows are CM parameter vectors consistent with the data\n\n\n\n\n\n","title":"SmoreFit.posteriorSamples"},{"category":"method","location":"#SmoreFit.posteriorScore-Tuple{CMPosteriorResult, AbstractVector}","page":"Home","text":"posteriorScore(post::CMPosteriorResult, θ_cm::AbstractVector) -> Float64\nposteriorScore(post::CMPosteriorResult, queries::AbstractMatrix) -> Vector{Float64}\n\nConsistency score at one or more interior CM parameter points, using the same bridge that produced post. The CM-side SM-parameter CI bounds are interpolated across the CM grid; the data-side profile (post.data_profiles) is unchanged.\n\nqueries[k, :] is the k-th query point; the vector form is one query.\n\nRequires post.cm_sample isa GridCMSample and post.bridge in (:box_overlap, :data_trace_in_box).\n\nExample\n\npost = buildPosterior(sm, data, uq_results, cm_params, cm_prior)   # cm_params a regular grid\nposteriorScore(post, [θ1, θ2])\nposteriorScore(post, [θ1_a θ2_a; θ1_b θ2_b])   # batch\n\n\n\n\n\n","title":"SmoreFit.posteriorScore"},{"category":"method","location":"#SmoreFit.posteriorWeights-Tuple{CMPosteriorResult}","page":"Home","text":"posteriorWeights(r::CMPosteriorResult) -> Vector{Float64}\n\nConsistency scores normalized to sum to one — a simple graded posterior (discrete distribution) over the CM cohort points. All-zero if no cohort point is consistent.\n\nExample\n\npost = buildPosterior(sm, data, uq_results, cm_params, cm_prior; posterior = :graded)\nw    = posteriorWeights(post)   # weight per cohort point, sums to 1\n\n\n\n\n\n","title":"SmoreFit.posteriorWeights"},{"category":"method","location":"#SmoreFit.scoreGrid-Tuple{CMPosteriorResult}","page":"Home","text":"scoreGrid(r::CMPosteriorResult) -> Array{Float64}\n\nReshape scores onto the CM parameter grid. Requires a GridCMSample layout.\n\nExample\n\npost = buildPosterior(sm, data, uq_results, cm_params, cm_prior)   # cm_params a regular grid\nscoreGrid(post)   # Float64 array of size length.(cm_sample.axes)\n\n\n\n\n\n","title":"SmoreFit.scoreGrid"}]
}
